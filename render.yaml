services:
  - type: web
    name: edubot
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node_modules/.bin/serve -s dist -l $PORT
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production
